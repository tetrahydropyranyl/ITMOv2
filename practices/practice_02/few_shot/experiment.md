# Few-shot

- Артефакт Практики 1:
   - [tests_unit.md](../../practice_01/tests_unit.md)
 - Что хотим улучшить:
   - Полнота проверок: добавить пустой `diff`, негативный кейс отсутствия ключа `diff`, проверку ровно одного вызова `llm.generate`.
   - Точность Evidence: указать конкретные строки TRAINING_PR.diff.
   - Единообразие формата: сохранить таблицу и разделы без новых колонок.

## Примеры

### Хороший результат

- Пример A — усиление требования к вызову LLM (Стало, хорошо):
  Требование: Вызов LLM ровно один раз | Проверяем изолированно: `ReviewService.review` вызывает `llm.generate` один раз с точным промптом | Вход: `diff = "X"` | Ожидаемый результат: одна инвокация `llm.generate("Review this pull request and find problems:\nX")` | Evidence: app/review_service.py:19-21

- Пример B — негатив для отсутствующего ключа (Стало, хорошо):
  Требование: Валидация входа (текущая реализация) | Проверяем изолированно: `create_review(payload)` при отсутствии `"diff"` | Вход: `payload = {}` | Ожидаемый результат: выбрасывается `KeyError` (фиксирует текущий риск) | Evidence: app/api.py:35-37

### Плохой результат

- Пример A — усиление требования к вызову LLM (Было, слабо):
  Требование: Формирование промпта | Проверяем: строка промпта | Ожидаем: LLM вызван | Evidence: app/review_service.py

- Пример B — негатив для отсутствующего ключа (Было, слабо):
  Отсутствует кейс про `payload["diff"]`.

## Запрос

См. промпт: [prompt.md](./prompt.md)

## Что получили
 
Сформирован артефакт: [tests_unit_v2.md](./tests_unit_v2.md)
Результат: расширена таблица единичных проверок, добавлены негативный и граничный кейсы, уточнены Evidence до строк TRAINING_PR.diff, сохранён формат.

## Что изменили в исходном артефакте

- Файл и раздел:
- practices/practice_01/tests_unit.md - основная таблица Unit-проверок
- Изменение:
- Добавлены строки про пустой `diff`, ровно один вызов `llm.generate`, негативный кейс отсутствующего ключа `diff`; уточнены Evidence до конкретных строк.
- Как проверили:
- Сопоставили ожидаемые поведения с TRAINING_PR.diff (app/review_service.py:19-22; app/api.py:35-37) и контекстом.
- Что отклонили:
- Добавление новых колонок или изменений формата - оставлено исходным по требованиям.
